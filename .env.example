# Public WebSocket base URL (NO trailing slash). The app will append /ws.
# Examples:
# - LAN (only same Wi‑Fi devices can use it):
#   VITE_WS_URL=ws://192.168.1.111:3001
# - Public host (cloud):
#   VITE_WS_URL=wss://your-cloud-ws.example.com
# Optional: Legacy WebSocket backend URL (if you deploy/run server.js yourself)
VITE_WS_URL=
